
import fetch from 'node-fetch'

const ids = []
const launches = fetch('https://api.spacexdata.com/v3/history', { 
	headers: { 'Content-Type': 'application/json' },
}).then(data => data.json());

const transform = function(launch) {
	return `🚀 ${launch.name}`
}

launches.then(data => console.log(data))
